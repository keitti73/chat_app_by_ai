# =================================================================
# AWS AppSync チャットアプリ 環境変数設定ファイル（Cognito認証対応版）
# =================================================================
# このファイルは設定の例を示すテンプレートです
# 実際に使う時は、このファイルをコピーして「.env」という名前にして
# 下の空欄に実際の値を入れてください
#
# 使い方：
# 1. このファイルをコピー： cp .env.example .env
# 2. Terraformでデプロイした後の出力値を下の空欄に入れる
# 3. アプリを起動すると、これらの設定が自動で読み込まれる

# ============ AppSync API 設定 ============

# GraphQL Endpoint（グラフQL エンドポイント）
# これはAWSのAppSyncサービスが作ったAPIのURLです
# フロントエンド（Reactアプリ）がサーバーと通信するためのアドレス
VITE_APPSYNC_GRAPHQL_ENDPOINT=

# AWS Region（AWSのリージョン）
# AWSのサービスを使う地域を指定します
# 日本なら「ap-northeast-1」、アメリカ東部なら「us-east-1」
# ここで指定した地域にデータベースやAPIが作られます
VITE_AWS_REGION=us-east-1

# ============ 認証設定 ============

# Authentication Type（認証タイプ）
# どのような方法でユーザーを認証するかを決めます
# AMAZON_COGNITO_USER_POOLS = Cognitoを使った高度な認証（推奨）
# API_KEY = APIキーを使った簡単な認証
VITE_APPSYNC_AUTH_TYPE=AMAZON_COGNITO_USER_POOLS

# AppSync API Key（AppSync APIキー）※バックアップ用
# Cognito認証が主ですが、念のためAPIキーも設定できます
VITE_APPSYNC_API_KEY=

# ============ Cognito 認証設定 ============

# Cognito User Pool ID（ユーザープールID）
# ユーザーの情報（メール、パスワードなど）を管理するデータベースのID
# 例: us-east-1_xxxxxxxxx の形式
VITE_COGNITO_USER_POOL_ID=

# Cognito User Pool Client ID（ユーザープールクライアントID）
# アプリがCognitoと通信するためのクライアントID
# 長い英数字の文字列になります
VITE_COGNITO_USER_POOL_CLIENT_ID=

# Cognito Identity Pool ID（アイデンティティプールID）
# 認証されたユーザーに一時的なAWS権限を与えるためのID
# 例: us-east-1:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx の形式
VITE_COGNITO_IDENTITY_POOL_ID=

# Cognito Region（Cognitoのリージョン）
# 通常はAWS_REGIONと同じ値を設定します
VITE_COGNITO_REGION=us-east-1
